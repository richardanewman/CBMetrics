import { AverageEarningsPerSale } from './average-earnings-per-sale';

describe('AverageEarningsPerSale', () => {
  it('should create an instance', () => {
    expect(new AverageEarningsPerSale()).toBeTruthy();
  });
});
