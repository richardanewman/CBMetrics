import { InitialEarningsPerSale } from './initial-earnings-per-sale';

describe('InitialEarningsPerSale', () => {
  it('should create an instance', () => {
    expect(new InitialEarningsPerSale()).toBeTruthy();
  });
});
